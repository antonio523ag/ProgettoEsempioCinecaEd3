creare un'applicazione per la gestione e le recensioni dei viaggi fatti dagli utenti
l'utente avrà come attributi
	nome
	cognome
	email
	password
	ruolo (base,Admin)
	dataDiNascita (LocalDate)
ogni utente avrà n viaggi
il viaggio avrà
	utente
	meta
	valutazione (0-5)
	dataPartenza (LocalDate)
	dataRitorno (LocalDate)
	descrizione
la meta avrà (barcellona, milano,bucarest, canicattì,capracotta... ecc)
	nome
	nazionalità
	un elenco di tipologie

la tipologia avrà (mare, lago, montagna, deserto, vulcano...ecc)
	il tipo
	un elenco di mete

un utente base potrà
	inserire un viaggio, visualizzare i propri viaggi
	visualizzare le mete
	visualizzare le mete per singola tipologia
	visualizzare le 10 mete con recensione più alta
	visualizzare i viaggi per una singola meta

un utente admin potrà
	creare le tipologie
	creare le mete
	cancellare un viaggio
	cancellare un utente base
	(tutte le visuallizza dell'utente base)


Step implementazione Security
    1-> importo le dipendenze
    2-> implemento UserDetails nella Classe Utente
    3-> creo il service per la gestione del Token JWT
    4-> creo la classe di Configurazione per i bean per la security
    5-> creo il filter per JWT
    6-> creo la classe che gestisce la filterChain

dopo aver implementato la security

per ogni entità

{
creo i controller capendo così quali DTO mi servono per le request e quali per le response


creo i DTO
creo i mapper per la conversione tra DTO e Entity

creo i service per le operazioni CRUD

creo le repository con i metodi che mi servono

}